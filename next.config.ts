import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['cdn.pixabay.com', 'source.unsplash.com','images.unsplash.com','media.istockphoto.com'], // add more if needed
  },
};

export default nextConfig;
